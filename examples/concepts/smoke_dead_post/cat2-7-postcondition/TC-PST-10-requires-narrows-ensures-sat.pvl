//:: cases TC_PST_10_RequiresNarrowsEnsuresSat
//:: tools silicon
//:: verdict Pass

// TC-PST-10: Non-trivial precondition requires x > 10 combined with ensures x > 0.
// The postcondition x > 0 is satisfiable on its own (e.g., x=1) — checker must not fire.
// Normal verification also passes: x > 10 implies x > 0, body leaves x unchanged.
requires x > 10;
ensures x > 0;
void f(int x) { }
