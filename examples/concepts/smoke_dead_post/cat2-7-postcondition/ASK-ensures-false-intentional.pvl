//:: cases TC_PST_3_EnsuresFalseIntentional
//:: tools silicon
//:: verdict Pass

// TC-PST-3: ensures false is matched as BooleanValue(false) and skipped by the
// postSat checker (it is already always an error through other checks).
// requires false makes normal verification vacuous, so no other errors fire.

ensures false;
void f(int x) { }
