//:: cases TC_PST_7_EnsuresSatisfiableVoid
//:: tools silicon
//:: verdict Pass

// TC-PST-7: Void method — no \result variable. returnTypeOf returns None, so
// substituteResult is skipped. Postcondition n > 0 is satisfiable (e.g., n=1).
// Supervisor's fix inhales n > 0 from requires; postcondition n > 0 is also inhaled.
// State is satisfiable — postUnsatisfiable must not fire.
// Normal verification also passes: precondition n > 0 directly establishes the postcondition.
requires n > 0;
ensures n > 0;
void f(int n) { }
