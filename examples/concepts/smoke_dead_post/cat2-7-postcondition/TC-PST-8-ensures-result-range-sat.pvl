//:: cases TC_PST_8_EnsuresResultRangeSat
//:: tools silicon
//:: verdict Pass

// TC-PST-8: Range postcondition \result >= 0 && \result <= 1000 is satisfiable (e.g., result=500).
// The postSat checker must not fire.
ensures \result >= 0 && \result <= 1000;
int f(int x) {
    return 500;
}
