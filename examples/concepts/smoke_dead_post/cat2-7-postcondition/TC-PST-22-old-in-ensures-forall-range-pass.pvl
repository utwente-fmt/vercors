//:: cases TC_PST_22_OldInEnsuresForallRangePass
//:: tools silicon
//:: verdict Pass

// TC-PST-22: \old appears in the upper bound of a \forall range in an ensures
// clause — the exact pattern from ArrayList.java lines 137/160 that previously
// crashed with a Viper trigger consistency error.
// The postcondition is satisfiable, so the sat check must NOT fire.
requires n >= 0;
ensures (\forall int d = 0 .. \old(n); d >= 0);
void f(int n) { }
