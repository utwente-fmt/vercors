//:: cases PERF_SEQ_15loops
//:: tools silicon
//:: verdict Pass

// Performance: 15 sequential loops, same arithmetic invariant.
// checkInvSat: 15 independent FramedProofs — linear scaling test.
// detectDeadCode: 30 inline Refutes; later post-loop checks carry heavier accumulated state.
requires n >= 0;
void perf_seq_15(int n) {
    int i1 = 0;
    loop_invariant i1 >= 0 && i1 <= n;
    while (i1 < n) { i1 = i1 + 1; }
    int i2 = 0;
    loop_invariant i2 >= 0 && i2 <= n;
    while (i2 < n) { i2 = i2 + 1; }
    int i3 = 0;
    loop_invariant i3 >= 0 && i3 <= n;
    while (i3 < n) { i3 = i3 + 1; }
    int i4 = 0;
    loop_invariant i4 >= 0 && i4 <= n;
    while (i4 < n) { i4 = i4 + 1; }
    int i5 = 0;
    loop_invariant i5 >= 0 && i5 <= n;
    while (i5 < n) { i5 = i5 + 1; }
    int i6 = 0;
    loop_invariant i6 >= 0 && i6 <= n;
    while (i6 < n) { i6 = i6 + 1; }
    int i7 = 0;
    loop_invariant i7 >= 0 && i7 <= n;
    while (i7 < n) { i7 = i7 + 1; }
    int i8 = 0;
    loop_invariant i8 >= 0 && i8 <= n;
    while (i8 < n) { i8 = i8 + 1; }
    int i9 = 0;
    loop_invariant i9 >= 0 && i9 <= n;
    while (i9 < n) { i9 = i9 + 1; }
    int i10 = 0;
    loop_invariant i10 >= 0 && i10 <= n;
    while (i10 < n) { i10 = i10 + 1; }
    int i11 = 0;
    loop_invariant i11 >= 0 && i11 <= n;
    while (i11 < n) { i11 = i11 + 1; }
    int i12 = 0;
    loop_invariant i12 >= 0 && i12 <= n;
    while (i12 < n) { i12 = i12 + 1; }
    int i13 = 0;
    loop_invariant i13 >= 0 && i13 <= n;
    while (i13 < n) { i13 = i13 + 1; }
    int i14 = 0;
    loop_invariant i14 >= 0 && i14 <= n;
    while (i14 < n) { i14 = i14 + 1; }
    int i15 = 0;
    loop_invariant i15 >= 0 && i15 <= n;
    while (i15 < n) { i15 = i15 + 1; }
}
