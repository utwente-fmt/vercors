//:: cases PERF_BLOCKINV_perm_1field
//:: tools silicon
//:: verdict Pass

// Performance: par block invariant with one field permission.
// checkInvSat (par branch): FramedProof must inhale 1 Perm grant + 1 value constraint.
class PERF_BLOCKINV_perm_1field {
    int a;
}

context Perm(c.a, write) ** c.a >= 0;
void f(PERF_BLOCKINV_perm_1field c) {
    invariant inv (Perm(c.a, write) ** c.a >= 0) {
    }
}
