//:: cases PERF_BRANCH_5
//:: tools silicon
//:: verdict Pass

// Performance: 5 sequential if-else branches, all live (no precondition restricts x).
// detectDeadCode: 10 inline Refutes (2 per branch). Baseline for branch scaling tests.
// Compare to PERF-SEQ-1loop to see branch vs loop instrumentation cost difference.
void perf_branch_5(int x) {
    if (x > 0) { } else { }
    if (x > 10) { } else { }
    if (x > 100) { } else { }
    if (x > 1000) { } else { }
    if (x > 10000) { } else { }
}
