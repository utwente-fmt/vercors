//:: cases TC_PC_7_EqualityPinning
//:: tools silicon
//:: verdict Fail

// TC-PC-7: Equality pinning — requires x == 5 fixes x to exactly 5.
// Any branch on x != 5 is dead.
requires x == 5;
void f(int x) {
    if (x != 5) {
        return; // dead: x is always exactly 5
    }
}
