requires x > 0;
void f(int x) {
    if (x > 5) {
        x = x + 1; // live: x could be 6, 7, ...
    } else { 
        x = x + 1; // live: x could be 1, 2, ..., 5
    }
    //PROBLEM: else branch is always dead
}