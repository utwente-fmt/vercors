//:: cases TC_PC_10_IfWithoutElse
//:: tools silicon
//:: verdict Fail

// TC-PC-10: Structural variant — if without else.
// Dead then-branch with no else branch present.
requires x > 0;
void f(int x) {
    if (x < 0) {
        return; // dead: x > 0 AND x < 0 unsatisfiable
    }
}
