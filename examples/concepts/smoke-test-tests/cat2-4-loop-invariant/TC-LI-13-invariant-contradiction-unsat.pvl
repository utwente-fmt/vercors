//:: cases TC_LI_13_InvariantContradictionUnsat
//:: tools silicon
//:: verdict Fail

// Contradictory invariant (x > 0 && x < 0).
requires true;
void f(int x) {
    loop_invariant x > 0 && x < 0;
    while (x != 0) {
        x = x - 1;
    }
}
