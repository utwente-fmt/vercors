//:: cases TC_LI_9_PostLoopFalseInvariantAfterCascade
//:: tools silicon
//:: verdict Fail

// loop_invariant false — cascade suppresses body and post-loop checks.
void f(int x) {
    loop_invariant false;
    while (x > 0) {
        x = x - 1; // suppressed via cascade
    }
    int dead = x; // suppressed via cascade
}
