//:: cases TC_LI_2_LoopBodyDead
//:: tools silicon
//:: verdict Fail

// Loop body dead: precondition contradicts the loop condition.
requires x > 0;
void f(int x) {
    while (x < 0) {
        refute false; // dead: x > 0 AND x < 0 unsatisfiable
    }
}
