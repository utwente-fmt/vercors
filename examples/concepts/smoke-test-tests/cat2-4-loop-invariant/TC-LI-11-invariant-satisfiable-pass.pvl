//:: cases TC_LI_11_InvariantSatisfiablePass
//:: tools silicon
//:: verdict Pass

// Negative: invariant x >= 0 is satisfiable.
requires x >= 0;
void f(int x) {
    loop_invariant x >= 0;
    while (x < 10) { x = x + 1; }
}
