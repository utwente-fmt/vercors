//:: cases TC_AS_3_AssumesMutual
//:: tools silicon
//:: verdict Fail

// Neither assume alone is dead; together they contradict.
void f(int x) {
    assume x > 0;
    assume x < 0; // contradicts previous assume
    x = x + 1;
}
