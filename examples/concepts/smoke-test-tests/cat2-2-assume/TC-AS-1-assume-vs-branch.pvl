//:: cases TC_AS_1_AssumeVsBranch
//:: tools silicon
//:: verdict Fail

// assume mid-body contradicts the branch condition.
void f(int x) {
    assume x > 0;
    if (x < 0) {
        x = x - 1;
        assume false; // dead: assume x > 0 contradicts x < 0
    }
}
