//:: cases TC_CM_2_PreconditionInhale
//:: tools silicon
//:: verdict Fail

// Same as TC-CM-1 but via inhale.
requires x > 0;
void f(int x) {
    inhale x < 5;
    if (x > 10) {
        return; // dead: x in (0,5) but x > 10 impossible
    }
}
