//:: cases TC_CM_1_PreconditionAssume
//:: tools silicon
//:: verdict Fail

// Precondition + assume together make a branch dead.
requires x > 0;
void f(int x) {
    assume x < 10;
    if (x > 20) {
        refute false; // dead: x in (0,10) but x > 20 impossible
    }
}
