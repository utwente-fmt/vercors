//:: cases TC_CM_6_NegativeConsistent
//:: tools silicon
//:: verdict Pass

// Negative: multiple constructs, all consistent — branch is live.
requires x > 0;
void f(int x) {
    assume x < 100;
    if (x > 5) {
        refute false; // live: x could be 6..99, satisfying all constraints
    }
}
