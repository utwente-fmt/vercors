//:: cases TC_CA_9_InvNotEstablishedSuppressesBody
//:: tools silicon
//:: verdict Fail

// Unestablishable invariant suppresses both body and post-loop deadBranch.
// Expected errors: 1.
void f(int x) {
    loop_invariant false;
    while (x > 0) {
        x = x - 1; // body deadBranch suppressed — cascade artifact
    }
}
