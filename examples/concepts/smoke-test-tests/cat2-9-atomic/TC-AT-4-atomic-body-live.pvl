//:: cases TC_AT_4_AtomicBodyLive
//:: tools silicon
//:: verdict Pass

// Negative: atomic body is live, no false positive.
requires x > 0;
void f(int x) {
    invariant inv (x > 0) {
        atomic (inv) {
            assert x > 0;
        }
    }
}
