//:: cases TC_BI_5_NestedBlockInvariantUnsat
//:: tools silicon
//:: verdict Fail

// Nested block invariants — only the inner one is unsatisfiable.
requires n >= 0;
void f(int n) {
    invariant outer (n >= 0) {
        invariant inner (n > 0 && n < 0) {
        }
    }
}
