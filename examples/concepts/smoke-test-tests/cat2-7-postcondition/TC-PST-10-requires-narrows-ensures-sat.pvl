//:: cases TC_PST_10_RequiresNarrowsEnsuresSat
//:: tools silicon
//:: verdict Pass

// Postcondition satisfiable on its own; precondition is just narrower.
requires x > 10;
ensures \result < 0;
int f(int x) { return -x; }
