//:: cases TC_PST_22_OldInEnsuresForallRangePass
//:: tools silicon
//:: verdict Pass

// \old in a \forall range bound — previously crashed with a trigger error.
requires n >= 0;
ensures (\forall int d = 0 .. \old(n); d >= 0);
void f(int n) { }
