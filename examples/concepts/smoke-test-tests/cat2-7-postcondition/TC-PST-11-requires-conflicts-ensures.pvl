//:: cases TC_PST_11_RequiresConflictsEnsures
//:: tools silicon
//:: verdict Fail

// TC-PST-11: Postcondition x < 0 is independently satisfiable.
// PostSat check: inhale x < 0 → satisfiable → postUnsatisfiable must NOT fire.
// Normal verification fails because x is unconstrained and may be >= 0.
ensures x < 0;
void f(int x) { }
