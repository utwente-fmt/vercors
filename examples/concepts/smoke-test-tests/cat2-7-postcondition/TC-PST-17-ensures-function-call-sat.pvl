//:: cases TC_PST_17_EnsuresFunctionCallSat
//:: tools silicon
//:: verdict Pass

// Postcondition calls a function whose precondition foo's own precondition satisfies.

requires x > 10;
pure boolean checkPos(int x) = x > 10;

requires n > 10;
ensures checkPos(n);
void foo(int n) { }
