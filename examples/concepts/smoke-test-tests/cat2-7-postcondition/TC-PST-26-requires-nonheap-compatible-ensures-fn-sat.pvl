//:: cases TC_PST_26_RequiresNonHeapCompatibleEnsuresFnSat
//:: tools silicon
//:: verdict Pass

// Same as TC-PST-25 but the precondition and postcondition are compatible.

pure boolean isPos(int n) = n > 0;

requires n > 0;
ensures isPos(n);
void foo(int n) { }
