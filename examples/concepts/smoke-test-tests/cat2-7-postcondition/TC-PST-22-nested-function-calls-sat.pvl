//:: cases TC_PST_22_NestedFunctionCallsSat
//:: tools silicon
//:: verdict Pass

// Postcondition nests two pure function calls, satisfiable.

pure int increment(int n) = n + 1;

pure boolean isLargeEnough(int x) = x > 1;

requires n > 0;
ensures isLargeEnough(increment(n));
void foo(int n) { }
