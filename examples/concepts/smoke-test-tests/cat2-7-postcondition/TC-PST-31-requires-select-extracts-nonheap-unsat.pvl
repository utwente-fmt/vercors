//:: cases TC_PST_31_RequiresSelectExtractsNonHeapUnsat
//:: tools silicon
//:: verdict Fail

// Precondition is a ternary mixing permission and a pure fact; extraction must split it.
class TC_PST_31 {
    int x;

    requires a > 0 ? (Perm(x, write) ** a > 100) : a > 100;
    ensures a > 0;
    ensures a < 10;
    void foo(int a) {
        assume false;
    }
}
