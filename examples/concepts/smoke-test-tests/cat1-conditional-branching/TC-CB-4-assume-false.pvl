//:: cases TC_CB_4_AssumeFalse
//:: tools silicon
//:: verdict Fail

// assume false narrows the state before the branch is reached.
void f(int x) {
    assume false; // state immediately contradictory
    if (x > 0) {
        x = x + 1; // dead: anything after assume false is unreachable
    }
}
