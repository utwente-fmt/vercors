//:: cases TC_CB_2_SelfContradicting
//:: tools silicon
//:: verdict Fail

// Branch condition contradicts itself, no specification needed.
void f(int x) {
    if (x > 0 && x < 0) {
        x = x + 1; // dead: impossible condition
    }
}
