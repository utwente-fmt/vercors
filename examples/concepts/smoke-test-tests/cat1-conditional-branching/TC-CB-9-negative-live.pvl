//:: cases TC_CB_9_NegativeLive
//:: tools silicon
//:: verdict Pass

// Negative: no specification, branch is live.
void f(int x) {
    if (x > 0) {
        x = x +1; // live: x could be anything > 0
    }
}
