//:: cases TC_CB_3_TautologyElseDead
//:: tools silicon
//:: verdict Fail

// Tautological condition — else-branch is dead.
void f(int x) {
    if (true) {
        x = x + 1; // live: condition always true
    } else {
        x = x + 2; // dead: else never reached
    }
}
