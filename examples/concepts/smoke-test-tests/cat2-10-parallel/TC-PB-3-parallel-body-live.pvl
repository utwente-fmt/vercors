//:: cases TC_PB_3_ParallelBodyLive
//:: tools silicon
//:: verdict Pass

// TC-PB-3: Par block with non-empty range — body is reachable. No deadBranch.
requires n > 0;
void f(int n) {
    par (int i = 0 .. n) {
        assert i >= 0;
    }
}
