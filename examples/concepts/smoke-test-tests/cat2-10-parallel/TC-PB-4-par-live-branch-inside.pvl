//:: cases TC_PB_4_ParLiveBranchInside
//:: tools silicon
//:: verdict Pass

// Negative: branch inside par body is live.
requires n > 0;
void f(int n) {
    par (int tid = 0 .. n) {
        if (tid < n) {
            assert tid >= 0;
        }
    }
}
