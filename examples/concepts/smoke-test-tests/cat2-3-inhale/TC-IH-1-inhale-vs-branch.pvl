//:: cases TC_IH_1_InhaleVsBranch
//:: tools silicon
//:: verdict Fail

// Same as TC-AS-1 but via inhale instead of assume.
void f(int x) {
    inhale x > 0;
    if (x < 0) {
        x = x + 1; // dead: inhale x > 0 contradicts x < 0
    }
}
