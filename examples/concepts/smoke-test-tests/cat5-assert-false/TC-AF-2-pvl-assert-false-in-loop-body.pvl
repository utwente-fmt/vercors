//:: cases TC_AF_2_PvlAssertFalseInLoopBody
//:: tools silicon
//:: verdict Fail

// Same as TC-AF-1 but the cutoff is inside a loop body.
void f(int x) {
    loop_invariant true;
    while (x < 10) {
        x = x + 1;
        assert false;    // assertFailed — block cutoff within body
        if (x > 5) {     // not instrumented (block cutoff)
            x = 0;
        }
    }
}
