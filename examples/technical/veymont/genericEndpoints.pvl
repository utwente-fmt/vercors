class Role<T> {
  T t;
  int i;

  T fromInt(int i);
  int toInt(T t);
}

adt A { }

adt B { }

seq_program genericTest() {
  endpoint a = Role<A>();
  endpoint b = Role<B>();

  seq_run {
    a.i := a.toInt(a.t);
    communicate a.i -> b.i;
    b.t := b.fromInt(b.i);
  }
}