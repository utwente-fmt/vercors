choreography Main() {
    endpoint a = Role();
    endpoint b = Role();
    endpoint c = Role();

	run {
		a.x := b.x; // Error, assignment from multiple sources.
	}
}

choreography Main2() {
    endpoint a = Role();
    endpoint b = Role();
    endpoint c = Role();

	run {
	    foo();
	}

	void foo() {
		a.x := c.x; // Error, assignment from multiple sources.
    }
}

class Role {
	int x;
}
