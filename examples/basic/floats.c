//:: case CFLoats
//:: tools silicon

void calculations(float a, float b) {
    float add = a + b;
    float sub = a - b;
    float prod = a * b;
    float div = a / b;
    float neg = -a;
    if(a < b || a > b) {

    }
}